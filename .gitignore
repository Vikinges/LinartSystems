hub/admin.json
hub/data/
hub/static/uploads/
hub/node_modules/
traefik/letsencrypt/acme.json
secrets/

# mobile/test artifacts
service2/data/submissions.json
service-sign/node_modules/
service2/test-server.log

# runtime data (service2/data is bind-mounted as the live data dir in prod —
# tracking it lets a checkout overwrite/delete production state)
service2/data/people.json

# local editor/agent config
.claude/
.env.local
